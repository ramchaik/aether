service: aether-s3proxy

provider:
  name: aws
  runtime: nodejs20.x
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  iam:
    role: arn:aws:iam::502413910473:role/LabRole

plugins:
  - serverless-esbuild
  - serverless-offline

custom:
  esbuild:
    bundle: true
    minify: true
    sourcemap: true
    exclude: ["aws-sdk"]
    target: "node20"
    define: { "require.resolve": undefined }
    platform: "node"
    concurrency: 10
    external:
      - mock-aws-s3
      - nock
    loader:
      ".html": "text"
  serverless-offline:
    httpPort: 9000

functions:
  proxy:
    handler: src/handler.proxy
  events:
    - http:
        path: proxy
        method: get
